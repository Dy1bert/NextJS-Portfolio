import React from 'react';
import { ProjectList } from "@/app/ProjectList";

const ProjectShowcase = () => {
    return (
        <section className="w-full h-[87.5vh] flex flex-col items-center justify-center bg-background-a text-center">
            <h1 className="text-6xl font-bold text-white">
                Test Section
            </h1>

            <h2 className="mt-4 text-md text-gray-400 max-w-5xl">
                This will eventually be the project showcase!
            </h2>
        </section>
    );
};

export default ProjectShowcase;